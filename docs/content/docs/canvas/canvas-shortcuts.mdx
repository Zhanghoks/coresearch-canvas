---
title: Canvas Shortcuts
description: Common mouse and keyboard controls
---

# Canvas Shortcuts

## View

- Drag empty canvas space to pan.
- Use the mouse wheel or zoom slider to zoom.
- Use Reset View to restore the default zoom and centered position.

## Selection

- `Ctrl / Cmd` + drag: box-select multiple nodes.
- `Shift / Ctrl / Cmd` + click: add or remove a node from the selection.
- `Ctrl / Cmd` + `A`: select all nodes.
- `Esc`: clear the selection and close the active overlay.

## Editing

- `Ctrl / Cmd` + `C`: copy selected nodes.
- `Ctrl / Cmd` + `V`: paste nodes.
- `Ctrl / Cmd` + `G`: group selected nodes.
- `Ctrl / Cmd` + `Shift` + `G`: ungroup the selection.
- `Delete / Backspace`: delete selected nodes or edges.
- `Ctrl / Cmd` + `Z`: undo.
- `Ctrl / Cmd` + `Shift` + `Z`, or `Ctrl / Cmd` + `Y`: redo.

Drag image, video, or audio files onto the canvas to create nodes, or use Import Image from the toolbar. Undo and redo cover nodes, edges, the viewport, background mode, and assistant sessions; they do not cover the project name, account state, or global settings.
